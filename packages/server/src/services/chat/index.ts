/**
 * Публичная часть чата: то, что стоит между виджетом на чужом сайте
 * и движком диалога.
 *
 *   rate-limit.ts — ограничение частоты обращений по сессии и по адресу.
 *
 * Сам разговор ведёт `services/dialog`, HTTP-слой — `routes/chat.routes.ts`.
 */

export * from './rate-limit.js'
