/**
 * Демонстрационные данные.
 *
 *   assets.ts — файлы комплекта: выгрузка, планировки, документ базы знаний;
 *   seed.ts   — засев: фид → квартиры → карточки ЖК → база знаний → настройки.
 *
 * Нужны, чтобы проект можно было показать до появления настоящих фидов и
 * ключа Anthropic. Всё работает без интернета: файлы лежат в проекте, а
 * раздаёт их сам сервер (`routes/demo.routes.ts`).
 */

export * from './assets.js'
export * from './seed.js'
